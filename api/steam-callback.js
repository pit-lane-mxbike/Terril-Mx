import crypto from 'crypto';
const REDIRECT_PAGE='/';const SECRET=process.env.SESSION_SECRET||'change-me-in-vercel-env';export default async function handler(req,res){const query=req.query;if(query['openid.mode']!=='id_res'){return res.redirect(`${REDIRECT_PAGE}?steam_error=1`);}const verifyParams=new URLSearchParams();for(const key in query){if(key.startsWith('openid.')){verifyParams.append(key,query[key]);}}verifyParams.set('openid.mode','check_authentication');let steamResponseText;try{const steamRes=await fetch('https://steamcommunity.com/openid/login',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:verifyParams.toString()});steamResponseText=await steamRes.text();}catch(err){return res.redirect(`${REDIRECT_PAGE}?steam_error=1`);}if(!steamResponseText.includes('is_valid:true')){return res.redirect(`${REDIRECT_PAGE}?steam_error=1`);}const claimedId=query['openid.claimed_id']||'';const match=claimedId.match(/\/openid\/id\/(\d+)$/);if(!match){return res.redirect(`${REDIRECT_PAGE}?steam_error=1`);}const steamId64=match[1];const signature=crypto.createHmac('sha256',SECRET).update(steamId64).digest('hex');const token=`${steamId64}.${signature}`;const maxAge=60*60*24*30;res.setHeader('Set-Cookie',`terrilmx_session=${token}; Max-Age=${maxAge}; Path=/; HttpOnly; Secure; SameSite=Lax`);return res.redirect(REDIRECT_PAGE);}
